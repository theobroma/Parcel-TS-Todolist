import * as React from 'react';
import MainAppContainer from './#/#/Main';

export default function App() {
  return (
    <React.StrictMode>
      <MainAppContainer />
    </React.StrictMode>
  );
}
